{
  "name": "chippy-web",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "homepage": "https://djrideout.github.io/chippy/",
  "scripts": {
    "dev": "vite --host",
    "dev:full": "cargo run --package wbg && vite --host",
    "build": "vite build",
    "build:wasm": "cargo run --package wbg",
    "build:full": "cargo run --package wbg && vite build",
    "build:gh-pages": "npm run build:full -- --outDir ../../docs --emptyOutDir",
    "preview": "vite preview --host"
  },
  "devDependencies": {
    "less": "^4.2.0",
    "typescript": "^5.5.3",
    "vite": "^5.4.8"
  }
}
